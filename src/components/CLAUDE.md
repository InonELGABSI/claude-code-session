# Components
- Use functional React components with TypeScript.
- Keep component files in PascalCase (`ScoreBoard.tsx`, `ClickButton.tsx`).
- Keep props small and explicit; define a props type/interface in the same file.
- Keep components presentational when possible; put game state logic in hooks/context.
